package com.booker_app.backend_service.models;

public enum AppointmentStatus {
    PENDING, CONFIRMED, CANCELLED, COMPLETED, NO_SHOW, IN_PROGRESS, FAILED, EXPIRED, RESCHEDULED, WAITLISTED, CHECKED_IN, CHECKED_OUT
}
